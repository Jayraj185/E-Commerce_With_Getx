class HomeModel {
  String? name, description, image;
  double? price, TotalPrice;
  int? quantity;

  HomeModel({
    this.name,
    this.description,
    this.image,
    this.price,
    this.TotalPrice,
    this.quantity,
  });
}
